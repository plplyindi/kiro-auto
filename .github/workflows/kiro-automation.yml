name: Claude Code 自动化任务

on:
  # 每天下午5点运行 (北京时间 17:00 = UTC 09:00)
  schedule:
    - cron: '0 9 * * *'

  # 也可以手动触发
  workflow_dispatch:

jobs:
  run-claude-tasks:
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 安装 Claude Code CLI
        run: |
          echo "开始安装 Claude Code CLI..."
          curl -fsSL https://claude.ai/install.sh | bash
          # 添加到 PATH
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: 验证 Claude Code 安装
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          claude --version

      - name: 配置 Claude Code
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
        run: |
          echo "配置 Claude Code..."

          # 创建配置目录
          mkdir -p ~/.claude

          # 1. 创建 settings.json
          cat > ~/.claude/settings.json << 'EOF'
          {
            "env": {
              "ANTHROPIC_BASE_URL": "${ANTHROPIC_BASE_URL:-https://api.anthropic.com}",
              "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}"
            }
          }
          EOF

          # 替换环境变量
          sed -i "s|\${ANTHROPIC_BASE_URL:-https://api.anthropic.com}|${ANTHROPIC_BASE_URL:-https://api.anthropic.com}|g" ~/.claude/settings.json
          sed -i "s|\${ANTHROPIC_API_KEY}|${ANTHROPIC_API_KEY}|g" ~/.claude/settings.json

          # 2. 创建 config.json
          cat > ~/.claude/config.json << EOF
          {
            "primaryApiKey": "${ANTHROPIC_API_KEY}"
          }
          EOF

          # 3. 创建 .claude.json
          cat > ~/.claude.json << 'EOF'
          {
            "hasCompletedOnboarding": true
          }
          EOF

          echo "✅ Claude Code 配置完成"
          echo "配置文件："
          echo "  - ~/.claude/settings.json"
          echo "  - ~/.claude/config.json"
          echo "  - ~/.claude.json"

      - name: 执行 Claude Code 任务（带重试）
        env:
          MAX_RETRIES: 3
          RETRY_DELAY: 10
        run: |
          export PATH="$HOME/.local/bin:$PATH"

          echo "========================================="
          echo "   Claude Code 自动化任务开始"
          echo "========================================="
          echo "北京时间: $(TZ='Asia/Shanghai' date)"
          echo "UTC时间: $(date)"
          echo ""

          # 检查任务文件
          if [ ! -f ".kiro/specs/tasks.md" ]; then
            echo "❌ 错误：找不到任务文件 .kiro/specs/tasks.md"
            exit 1
          fi

          echo "📋 任务文件内容："
          cat .kiro/specs/tasks.md
          echo ""
          echo "========================================="
          echo ""

          RETRY_COUNT=0
          SUCCESS=false

          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            RETRY_COUNT=$((RETRY_COUNT + 1))
            echo "🚀 [尝试 $RETRY_COUNT/$MAX_RETRIES]"
            echo ""

            # 执行 Claude Code（-p 表示 print/非交互模式）
            if claude -p "$(cat .kiro/specs/tasks.md)"; then
              echo ""
              echo "✅ 执行成功！"
              SUCCESS=true
              break
            else
              EXIT_CODE=$?
              echo ""
              echo "❌ 执行失败 (错误码: $EXIT_CODE)"
              if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
                echo "⏳ 等待 $RETRY_DELAY 秒后重试..."
                sleep $RETRY_DELAY
                echo ""
              fi
            fi
          done

          echo ""
          echo "========================================="
          if [ "$SUCCESS" = true ]; then
            echo "   ✅ 任务完成"
            echo "========================================="
            echo "结束时间: $(TZ='Asia/Shanghai' date)"
            exit 0
          else
            echo "   ❌ 任务失败（已重试 $MAX_RETRIES 次）"
            echo "========================================="
            echo "结束时间: $(TZ='Asia/Shanghai' date)"
            exit 1
          fi

      - name: 上传任务结果
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: claude-results-${{ github.run_number }}
          path: |
            *.txt
            *.log
            *.md
          retention-days: 7

      - name: 发送通知（成功）
        if: success()
        run: |
          echo "✅ Claude Code 任务执行成功！"
          echo "运行编号: ${{ github.run_number }}"
          echo "完成时间: $(TZ='Asia/Shanghai' date)"

      - name: 发送通知（失败）
        if: failure()
        run: |
          echo "❌ Claude Code 任务执行失败！"
          echo "运行编号: ${{ github.run_number }}"
          echo "请检查 Actions 日志"
